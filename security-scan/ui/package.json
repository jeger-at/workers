{
  "name": "@iii-workers/security-scan-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node --test src/page/*.test.mjs"
  },
  "dependencies": {
    "@iii-dev/console-ui": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.2"
  }
}
